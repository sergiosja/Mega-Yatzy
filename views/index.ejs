<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title> Login | Mega Yatzy </title>
    <link rel="stylesheet" type="text/css" href="/CSS/base.css">
</head>
<body>
    <h1 class="title"> Log in </h1>

    <!-- If the succesfully signs up or logs out -->
    <div class="center" id="success">
        <ul>
            <% if (messages.success) { %>
                <li> 
                    <%= messages.success %>
                </li>
            <% } %>
        </ul>
    </div>

    <!-- If the user fails to log in -->
    <div class="center" id="fail">
        <ul>
            <% if (messages.error) { %>
                <li>
                    <%= messages.error %>
                </li>
            <% } %>
        </ul>
    </div>

    <form id="log" action="/" method="post">
        <div class="login">
            <input type="text" id="username" name="username" placeholder="Username">
            <input type="password" id="password" name="password" placeholder="Password">
        </div>


        <div class="center">
            <button form="log" type="submit" class="menu"> Log in </button>
        </div>
    </form>
    
    <div class="center">
        <button class="menu"> <a href="/signup"> No user? Sign up here </a> </button>
    </div>
</body>
</html>